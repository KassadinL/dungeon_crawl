defmodule DungeonCrawlTest do
  use ExUnit.Case
  doctest DungeonCrawl

  test "greets the world" do
    assert DungeonCrawl.hello() == :world
  end

  test "another greets the world" do
    assert DungeonCrawl.hello() == :world
  end
end
